The code provided below moves a selected joint incrementally.
Before running the code make sure your calibration directory and config information are correct.
disable_torque_on_disconnect allows to either keep the final joint positions (False) or release the joint locks (True)


